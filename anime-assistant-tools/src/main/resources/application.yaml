server:
  port: 10011

spring:
  application:
    name: anime-assistant-tools
  profiles:
    active: dev
  mvc:
    pathmatch:
      matching-strategy: ant_path_matcher
  # 数据库配置
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://lzynb.com.cn:15432/yhy-yyds
    username: course-design
    password: course-design2023

mybatis-plus:
  type-enums-package: edu.whu.model.common.enumerate
  type-handlers-package: edu.whu.handler

